/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RoomtypemainService } from './roomtypemain.service';

describe('Service: Roomtypemain', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RoomtypemainService]
    });
  });

  it('should ...', inject([RoomtypemainService], (service: RoomtypemainService) => {
    expect(service).toBeTruthy();
  }));
});
