const router = require("express").Router();

// const roomtypeCon = require("../controllers/roomtypeCon");

// // Thêm loại phòng
// router.post("/", roomtypeCon.addRoomtype);

// // Lấy tất cả loại phòng
// router.get("/", roomtypeCon.getAllRoomtype);

// // Lấy loại phòng theo ID
// router.get("/:id", roomtypeCon.getAnRoomtype);

// // Cập nhật loại phòng
// router.put("/:id", roomtypeCon.updateRoomtype);

// // Xóa loại phòng
// router.delete("/:id", roomtypeCon.deleteRoomtype);

module.exports = router;
